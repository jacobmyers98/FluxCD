---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: homarr-policy
  namespace: infrastructure
spec:
  imageRepositoryRef:
    name: homarr
  policy:
    semver:
      range: ">=1.45.0"
