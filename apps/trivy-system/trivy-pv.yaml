apiVersion: v1
kind: PersistentVolume
metadata:
  name: trivy-server-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /home/server/docker/trivy
  storageClassName: local-storage
